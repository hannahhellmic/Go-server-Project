package main

import (
	b "bufio"
	f "fmt"
	"os"
	"strconv"
	"strings"
)

func bubbleSort(arr []int) []int {
	for i := 0; i < len(arr); i++ {
		for j := 0; j < len(arr)-i-1; j++ {
			if arr[j] > arr[j+1] {
				arr[j], arr[j+1] = arr[j+1], arr[j]
			}
		}
	}
	return arr
}

func main() {
	reader := b.NewReader(os.Stdin)
	s, _ := reader.ReadString('\n')
	s = strings.TrimSpace(s)
	a := strings.Split(s, " ")
	var arr []int
	for _, v := range a {
		n, err := strconv.Atoi(v)
		if err == nil {
			arr = append(arr, n)
		}
	}
	f.Print(bubbleSort(arr))
}
