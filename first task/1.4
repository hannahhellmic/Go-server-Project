package main

import f "fmt"

func getMax(a int, b int, c int) int {
	if a >= b && a >= c {
		return a
	} else if b >= a && b >= c {
		return b
	} else {
		return c
	}
}

func main() {
	var a, b, c int
	f.Scan(&a, &b, &c)
	f.Print(getMax(a, b, c))
}
