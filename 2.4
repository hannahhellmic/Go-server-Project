package main

import (
	"bufio"
	"fmt"
	"os"
	"strings"
)

// first string - array, second - element to find in array

func main() {
	reader := bufio.NewReader(os.Stdin)
	s, _ := reader.ReadString('\n')
	s = strings.TrimSpace(s)
	a := strings.Split(s, " ")
	var arr []string
	for _, v := range a {
		arr = append(arr, v)
	}

	var inArray bool = false
	var elem string
	fmt.Scan(&elem)
	for _, el := range arr {
		if el == elem {
			inArray = true
			break
		}
	}

	if inArray {
		fmt.Println("Yes")
	} else {
		fmt.Println("No")
	}
}
